<UserControl x:Class="ClienteAhorcado.Vistas.JuegoAhorcadoUserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClienteAhorcado.Vistas"
             Width="800" Height="600">

    <Grid Margin="10" Background="#FF02210D">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Título -->
            <RowDefinition Height="Auto"/>
            <!-- Imagen -->
            <RowDefinition Height="Auto"/>
            <!-- Palabra oculta -->
            <RowDefinition Height="*"/>
            <!-- Botones letras + chat -->
            <RowDefinition Height="Auto"/>
            <!-- Estado del juego -->
        </Grid.RowDefinitions>

        <Button x:Name="btnVolverMenu"
        Content="Volver al Menú"
        Width="180"
        Height="40"
        Visibility="Collapsed"
        HorizontalAlignment="Left"
        VerticalAlignment="Bottom"
        Margin="290,0,0,42"
        
        Click="BtnVolverMenu_Click" Grid.Row="3"/>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Juego del Ahorcado" 
                   FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10" FontFamily="Showcard Gothic" Foreground="White"/>

        <!-- Imagen del ahorcado -->
        <Image x:Name="imgAhorcado" Grid.Row="1" Height="200" Width="200" 
               Source="/Images/6.png" HorizontalAlignment="Center" Margin="0,10"/>

        <!-- Palabra oculta -->
        <StackPanel x:Name="stackPalabra" Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="10" />

        <!-- Letras y Chat -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Botones de letras -->
            <WrapPanel x:Name="wrapLetras" Grid.Column="0" Margin="10" VerticalAlignment="Top">
                <!-- Se agregan dinámicamente los botones con código -->
            </WrapPanel>

            <!-- Chat -->
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="8" Margin="10">
                <StackPanel>
                    <TextBlock x:Name="Chat" Text="Chat privado" FontWeight="Bold" Margin="0,0,0,5" Foreground="#FFFBF5F5"/>
                    <ScrollViewer Height="200">
                        <!-- Cambié TextBlock por TextBox para poder usar ScrollToEnd -->
                        <TextBox x:Name="txtChat" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
                    </ScrollViewer>
                    <DockPanel Margin="0,5,0,0">
                        <TextBox x:Name="txtMensaje" Width="150" Height="25" DockPanel.Dock="Left"/>
                        <Button Content="Enviar" Click="BtnEnviar_Click" Width="60" Height="25" Margin="5,0,0,0"/>
                    </DockPanel>
                </StackPanel>
            </Border>
        </Grid>
        <Button Name="btnCancelarPartida" Content="Cancelar Partida" Click="BtnCancelarPartida_Click" Margin="40,168,619,27" Grid.Row="3" Background="#FFD81111"/>
        <!-- Estado del juego -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock x:Name="lblIntentosRestantes" Text="Intentos restantes: " FontWeight="Bold" Foreground="#FFF9F7F7"/>
            <TextBlock x:Name="txtIntentosRestantes" Margin="5,0"/>
            <TextBlock x:Name="lblLetrasUsadas" Text=" | Letras usadas: " FontWeight="Bold" Margin="15,0,0,0" Foreground="#FFF3F0F0"/>
            <TextBlock x:Name="txtLetrasUsadas" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
