<UserControl x:Class="ClienteAhorcado.Vistas.IniciarSesionUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="461" Width="380"
        Background="#f0f2f5">
    <Border Background="White" CornerRadius="12" Margin="20" >
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Encabezado -->
            <StackPanel Grid.Row="0" Margin="0,10,0,20" HorizontalAlignment="Center">
                <TextBlock x:Name="tblockTitulo" Text="{DynamicResource InicioSesion_Titulo}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" TextAlignment="Center"/>
                <TextBlock x:Name="tblockSubtitulo" Text="{DynamicResource InicioSesion_Descripcion}" FontSize="14" Foreground="Gray" TextAlignment="Center"/>
            </StackPanel>
            <!-- Contenido -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Width="280">
                <TextBlock x:Name="tblockCorreo" Text="{DynamicResource Glb_CorreoUsuario}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="tbCorreo" Height="35" Padding="10,5,10,10" 
                         Tag="" />
                <TextBlock x:Name="tblockErrorCorreo" Text="" Foreground="Red"/>
                <TextBlock x:Name="tblockContrasena" Text="{DynamicResource Glb_Contrasenia}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <Grid>
                    <PasswordBox x:Name="pbPassword" Height="35" Padding="10" 
                             Tag="*" Margin="0,0,40,5" Visibility="Visible" PasswordChanged="PbPassword_PasswordChanged"/>
                    <TextBox x:Name="tbPasswordVisible" Height="35" Margin="0,0,40,5" Padding="10,5,10,10"
                             Tag="" VerticalAlignment="Center" Visibility="Collapsed" TextChanged="TbPasswordVisible_TextChanged"/>
                    <Button x:Name="btnVerPassword" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center" Click="BtnVerPassword_Click" Content="👁"/>
                </Grid>
                <TextBlock x:Name="tblockErrorPassword" Text="" Margin="0,0,0,5" Foreground="Red" VerticalAlignment="Bottom"/>
                <Button x:Name="btnIniciarSesion" Content="{DynamicResource InicioSesion_Boton_Iniciar}"
                        Click="BtnIniciarSesion_Click"
                        Style="{StaticResource EstiloBotonPrincipalSinIcono}"
                        Height="40" Margin="0,0,0,10"/>
                <Button  x:Name="btnRegistrarse" Content="{DynamicResource InicioSesion_Boton_Registrar}"
                        Click="BtnRegistrarse_Click"
                        Style="{StaticResource EstiloBotonSecundarioSinIcono}"
                        Height="40" Background="Transparent" BorderBrush="#3498db" Foreground="#3498db"/>
                <TextBlock x:Name="tblockSelectIdioma" Text="{DynamicResource Glb_Idioma}" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <ComboBox x:Name="cmbIdioma" 
                         SelectionChanged="cmbIdioma_SelectionChanged"
                         Tag="{DynamicResource Glb_Idioma}"
                         Width="100"
                         Height="30" Background="Transparent" BorderBrush="#3498db" Foreground="#3498db">
                    <ComboBoxItem Content="{DynamicResource Glb_Espanol}" Tag="es"/>
                    <ComboBoxItem Content="{DynamicResource Glb_Ingles}" Tag="en"/>
                </ComboBox>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
